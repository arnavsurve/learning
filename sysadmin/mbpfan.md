#linux #ubuntu #server #mac

# `mbpfan` to enable macbook fans

```bash
sudo apt install mbpfan
sudo systemctl enable mbpfan
sudo systemctl start mbpfan
```

Configuration is done in `/etc/mbpfan.conf`
